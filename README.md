# takwin
🚀 Modern C/C++ build tool with TOML config files. Cross-platform (Windows/Linux/macOS), multiple compilers (GCC/Clang/MSVC), glob patterns, and zero dependencies. Migrated from Python to Go for better performance.
